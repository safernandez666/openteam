Translate Figma designs to code.

- Match spacing, colors, and typography exactly as specified in the design
- Use the design's layer names as component/class names when sensible
- Implement responsive behavior even if only one breakpoint is shown
- Extract repeated patterns into reusable components
- Use CSS variables or theme tokens for colors and spacing from the design system
- Flag any ambiguities in the design rather than guessing

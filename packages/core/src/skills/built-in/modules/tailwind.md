Use Tailwind CSS for styling.

- Use utility classes instead of custom CSS when possible
- Follow the project's tailwind.config for theme tokens (colors, spacing, fonts)
- Use responsive prefixes (sm:, md:, lg:) for adaptive layouts
- Prefer @apply in component CSS only when utility strings get very long
- Use the cn() or clsx() helper for conditional classes
